import React from "react";
import { Button } from "../components";

const Home: React.FC = () => {
  return (
    <Button props variant="contained" size="large">
      Hello from Home
    </Button>
  );
};

export default Home;
